import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#eceff1',
    padding: 10,
    paddingBottom: 0,
    paddingTop: 0,
    margin: 5,
    borderRadius: 8,
  },
});
