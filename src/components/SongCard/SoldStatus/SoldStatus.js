import React from 'react';
import {View, Text} from 'react-native';
import styles from './SoldStatus.styles';

const SoldStatus = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}> TÜKENDİ </Text>
    </View>
  );
};

export default SoldStatus;
